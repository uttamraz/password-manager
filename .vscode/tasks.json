{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/password_manager.cpp",
                "-o",
                "${workspaceFolder}/password_manager.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
 }
 